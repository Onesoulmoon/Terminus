package com.necroware.terminusplayer.ui.theme

import androidx.compose.ui.graphics.Color

// Core terminal palette — dark grey base with cream body text.
val TerminalBlack = Color(0xFF121212)
val TerminalSurface = Color(0xFF1A1A1A)
val TerminalSurfaceElevated = Color(0xFF232323)
val TerminalBorder = Color(0xFF3A3A3A)
val TerminalBorderBright = Color(0xFF555555)

val CreamPrimary = Color(0xFFF2ECD9)
val CreamDim = Color(0xFFC9C2AC)
val CreamFaint = Color(0xFF8A8676)

// SymbolAccent is the color for anything that's a glyph/icon/interactive
// marker rather than body text — nav icons, transport controls, the
// liked-heart, prompt carets, chart bars, etc. Orange is now the default;
// the others remain selectable later from Settings.
val OrangeAccent = Color(0xFFE8863A)
val PhosphorGreen = Color(0xFF4ADE80)
val CyanAccent = Color(0xFF7FD8D0)
val RedAccent = Color(0xFFD9645A)

val TerminalTextPrimary = CreamPrimary
val TerminalTextSecondary = Color(0xFFA6A28F)
val TerminalTextMuted = Color(0xFF6B675A)

val ChartGrid = Color(0xFF2E2E2A)
